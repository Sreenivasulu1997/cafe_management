# Cafe Management System ☕🍰

This is a **FastAPI-based Cafe Management System** with authentication, menu management, and Docker-ready deployment.  

---

## Features

- User Authentication (Admin / Staff)
- Menu management (CRUD)
- JWT token-based login
- Dockerized application
- Ready for cloud deployment (AWS ECS / EC2)
- CI/CD support via Jenkins pipeline

---

## Folder Structure


# Cafe Management System ☕🍰

This is a **FastAPI-based Cafe Management System** with authentication, menu management, and Docker-ready deployment.  

---

## Features

- User Authentication (Admin / Staff)
- Menu management (CRUD)
- JWT token-based login
- Dockerized application
- Ready for cloud deployment (AWS ECS / EC2)
- CI/CD support via Jenkins pipeline

---


---

## Installation (Local)

1. Clone repo:

```bash
git clone https://github.com/<your-username>/cafe-management.git
cd cafe-management

2. Create Virtual Environment

python -m venv .venv
source .venv/bin/activate  # Linux/Mac
.venv\Scripts\activate     # Windows


3. Install dependencies 

pip install -r requirements.txt


4. Run fastapi:

uvicorn app.main:app --reload


swagger UI: http://127.0.0.1:8000/docs


